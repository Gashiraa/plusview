json.extract! ware, :id, :project_id, :invoice_id, :customer_id, :quotation_id, :name, :comment, :quantity, :provider_discount, :margin, :unit_price, :status, :tva_rate, :total_cost, :created_at, :updated_at
json.url ware_url(ware, format: :json)
